# No environment variables are required for the default CKB Testnet demo.
#
# The app uses static configuration in lib/fiberConfig.ts and browser wallet
# connection through @ckb-ccc/connector-react.
